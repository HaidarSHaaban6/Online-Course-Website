import { contactInfo } from "../redux/slice/contactFormSlice"

const MessagesCard = ({firstName, lastName, email, phone, subject, message}: contactInfo) => {
  return (
    <div className="bg-white rounded-xl px-">

    </div>
  )
}

export default MessagesCard